# Reviewed decision index

This is a traceability aid, not another specification. It lists the durable
architecture decisions and points to their current normative home. Historical
review files remain useful evidence but may contain superseded proposals.

| Area | Reviewed decision | Normative home |
|---|---|---|
| Layer boundary | FLOW owns the portable boundary; Jig owns admission, resolution, authority, lifecycle, and history; runners retain internal control. | [Reviewed architecture §1](design-review/60-reviewed-architecture.md#1-the-architecture-in-one-view) |
| Package | `FLOW.md` is required; its exact-case filename identifies bounded Metadata/1, whose only required fields are `name` and `description`. Ordinary resources and zero or one obvious `flow.<suffix>` implementation need no manifest DSL or mandatory contract. Any future core vocabulary or semantics requires a discriminator which v1 rejects. | [Reviewed architecture §3](design-review/60-reviewed-architecture.md#3-flow-package1), [Package/1 §2](spec/package-format.md#2-flowmd-metadata1) |
| Instruction mode | A Markdown-only package requires an Agent; exact code wins by default; instruction fallback is explicit in both package and Binding and is selected only before launch. A projection-capable Agent provider receives bounded logical resources and returns one complete structured result through Agent Run without another protocol; the projection ABI is release-gated. | [Reviewed architecture §3.3](design-review/60-reviewed-architecture.md#33-instruction-and-exact-implementations), [Agent contracts §3.2](spec/agents-and-semantic-choice.md#32-instruction-conductor-mapping) |
| Runtime selection | FLOW owns no runtime profiles, commands, toolchain versions, or digests. One optional strict selector token narrows trusted host Runtime Adapters; native metadata owns native constraints. Planning pins one implementation recipe or exact unavailability per admission generation; Run/Mount owners derive exact launch after Backend-supervised pinned preparation and never reselect machinery or fallback branches. Ambiguity fails rather than invoking instruction fallback. | [Runtime Adapters/1](spec/runtime-adapters.md), [Project policy §2.2](spec/project-policy.md#22-admission-and-operational-readiness) |
| Values and schemas | FLOW JSON/1 is bounded. `input`, complete immutable Binding `settings`, and correlated `result` use the three optional conventional Schema/1 files, never shared runtime mailboxes. | [JSON/1](spec/json-values.md), [Schema/1](spec/schema-files.md) |
| Run protocol | Run/1 is one root Run per process over bounded full-duplex JSON-RPC stdio. Its only methods are `flow/run`, `flow/call`, `effect/call`, and notification-only `request/cancel`; the root implicitly owns every outbound operation. The closed candidate fixes the six-field invocation, call/result shapes, limits, errors, cancellation, and exit behavior without public Run, parent, provider, authority, or owner-request IDs. Its private TypeScript and Python candidate SDKs expose only the finite component-author projection. The Phase 1 two-peer, package, and instruction-restricted author/evaluator gates are complete. | [Run/1](spec/run-protocol.md), [Run SDK/1](spec/run-sdk.md), [machine schema](spec/machine/run-1.schema.json), [error registry](spec/machine/run-1-errors.json), [Phase 1 disposition](design-review/103-phase-1-flow-foundations.md), [Jig policy §5](design-review/60-reviewed-architecture.md#5-flow-run1) |
| Root execution | A `RootAdministration` object issued only to trusted host-side control-plane code outside FLOW activations exposes `startRun` and `runStatus` for one already-open project. It is not a FLOW capability; portable packages use Run/1 calls and effects. Submission carries one admitted target, bounded opaque idempotency key, and FLOW JSON/1 input; callers cannot add trigger data, configuration, authority, or host policy. Replay uses normalized-target and JSON/1 equality with durable first-allocation linearization. A closed authority rejects both methods while accepted Runs remain durable. The candidate returns a Run ID, pending/terminal status, and closed errors without exposing coordinator, admission, process, runtime, or Backend internals. Its private controller owns recoverable dispatch, launch, fencing, backing release, result admission, and terminal projection. It never publishes a terminal or releases protected Run backing before an exact Backend fence is confirmed; replacement coordinators inventory older work and the controller reacquires its owner before settling loss. Clean and independently authored packed-package consumer gates pass. | [Root Administration/1](spec/root-administration.md), [machine schema](spec/machine/root-administration-1.schema.json), [direct-root closure](design-review/142-direct-root-closure-repair.md), [boundary clarification](design-review/140-root-administration-and-bun-boundary.md), [controller checkpoint](design-review/138-private-root-administration-controller.md), [independent review](design-review/139-independent-root-administration-review.md), [Project policy §10](spec/project-policy.md#10-root-run-admission) |
| Foreground operation | One proof-host-only script keeps `plan`, explicitly confirmed `apply`, and `run` separate. Apply accepts admission and lock-repair Plans; run accepts zero or more explicit submissions, recovers/pumps/drains the existing root controller, deliberately processes supplied roots sequentially in this proof, and disposes its coordinator. Retained-source direct Python and Bun-to-Python composition plus lock repair leave zero Run cgroup/private-device residue. This is finite product-flow evidence, not a CLI, daemon, supervisor, Service host, or public API. | [finite foreground checkpoint](design-review/153-private-finite-foreground-checkpoint.md), [foreground dogfood](design-review/145-private-foreground-dogfood.md), [Root Administration/1](spec/root-administration.md) |
| Services | Service/1 is a first-class, separately conforming target. Its closed wire candidate adds only a pending long-lived JSON service with one fixed dependency set, one fixed export set, attributed Mount/invocation ownership, drain, loss, and cancellation. TypeScript and Python Provider SDKs and a private Jig Host preserve those owners without public Scope objects; the first independent Provider matrix passes. The private hosting substrate runs one exact admitted Bun Mount inside the Linux envelope and durably closes readiness, generation, owner-slot lease, invocation allocation/dispatch/terminal/closure, fencing, and ordered release. One finite normal-path witness now joins a pinned Bun root `effect/call` to that acknowledged generation beside a Python child and canonical Journal append; Root A/replay receives `1`, Root B receives `2`, roots release their leases before Mount finalization, and normal cleanup leaves no residue. Mixed provider/coordinator-loss recovery and complete Host/Provider conformance remain open. Shadow-first replacement requires compatible leases; conflicting leases drain and fence before replacement. | [Service/1](spec/service-protocol.md), [Service SDK/1](spec/service-sdk.md), [mixed checkpoint](design-review/155-private-mixed-composition-checkpoint.md), [hosting checkpoint](design-review/151-private-service-hosting-checkpoint.md), [milestone](design-review/106-service-wire-and-provider-sdk.md), [Reviewed architecture §6](design-review/60-reviewed-architecture.md#6-flow-service1) |
| Capability contracts | Ordinary Flow calls need no contract. A stable structured service/effect consumer carries the rare self-contained descriptor it expects and references its package-relative path. Compatibility remains exact URI, version, and derived full-descriptor digest; Jig records the digest in normalized state and lock rather than asking authors to copy it into `FLOW.md`. | [Capability Contract/1](spec/capability-contracts.md) |
| Bindings and direct targets | One immutable normalized Binding is a tailored configured use of an exact FLOW package revision or trusted host-capability export. A narrowly zero-configuration Run package is instead a tagged direct admitted Flow target; Jig never synthesizes a hidden Binding. Both enter the same admission, authority, idempotency, Hook, revocation, sandbox, and scheduling path and may be admitted ready or exactly unavailable. Package Bindings have no generic `grants`; the host-capability branch remains interface-gated. | [Project policy §2](spec/project-policy.md#2-one-closed-normalized-binding-union), [target disposition](design-review/107-project-authoring-sdk-slice.md) |
| Discovery and consent | Generated projects opt into shallow `flows` and `bindings` sources with one `discover("./root")` primitive; exact path arrays provide fail-closed membership, and discovery alone executes nothing. Authored values, descriptor-confined capture, retained Package/1 artifacts, deterministic linking/planning, a reviewed candidate, portable lock, and the compare-and-set admission head remain separate records. Resolution and apply never reopen visible source. Exact Run, Hook, and Service targets share this pipeline: direct and Binding Runs can execute; Hooks pin admitted revisions and half-open Journal intervals; Service Bindings can be admitted `READY` or `UNAVAILABLE`, and one exact `READY` Bun Service recipe now drives both the private Mount lifecycle and one normal Root-to-Service invocation path. A live root resolves its Service lease from its pinned Candidate/5 revision even after the current admission head moves. The private Plan/2 checkpoint classifies a retained Candidate/5 as unchanged, lock repair, or admission and applies an applicable Plan using only its digest. Authority-changing admission retains candidate-head and admission-head CAS; lock repair is rooted in the active admission and ignores inert candidate-head movement. Complete capture-to-plan acquisition, a closed public error wrapper, the remaining classifier/authority matrix, public Lock/1, and public plan/apply remain open. | [Project policy](spec/project-policy.md), [Project Authoring SDK/1](spec/project-sdk.md), [mixed checkpoint](design-review/155-private-mixed-composition-checkpoint.md), [private admission frontier](design-review/150-private-project-admission-frontier.md), [Plan/2 checkpoint](design-review/152-private-plan2-classification-and-apply-checkpoint.md), [Service hosting checkpoint](design-review/151-private-service-hosting-checkpoint.md), [Hook authoring correction](design-review/147-private-hook-authoring-boundary.md), [Hook runtime closure](design-review/148-private-hook-runtime-frontier.md), [capture boundary](design-review/108-project-capture-boundary.md), [retained aggregate](design-review/113-retained-project-aggregate.md), [runtime-support checkpoint](design-review/131-private-runtime-lease-observation.md), [READY admission checkpoint](design-review/134-private-ready-activation-admission.md), [direct-root closure](design-review/142-direct-root-closure-repair.md), [deterministic child closure](design-review/144-deterministic-child-flow-closure.md), [authoring-value schema](spec/machine/project-authoring-1.schema.json) |
| Lock bootstrap | A fresh unlocked project may omit `jig.lock`; the first plan proposes it. Locked operations reject absence or drift. Ordinary admission apply durably publishes this inert portable evidence before committing the local admission CAS; a crash between them leaves the lock pending against the complete old generation. Portable authority evidence is requested attachments and capability dependencies, never host policy or enforcement receipts. The implemented package-project projection proves canonical bytes and that exclusion boundary but is intentionally not the final public schema. Equal final activation meaning plus equal proposed lock and exact visible bytes is `unchanged` with no Plan. In update mode, only absent or drifted visible bytes produce a lock-repair Plan. Applying it by `planDigest` records an idempotent receipt without advancing admission, Hook time, or Service work; exact lock/no-receipt crash state converges only through explicit Plan replay. Equivalent repair Plans commute and may each record a receipt; candidate-head movement does not stale them, while active-admission movement does. The private v17 store rejects every alternate-version database or sidecar instead of migrating or choosing among authority sources. | [Project policy §7](spec/project-policy.md#7-consent-and-the-lock), [private admission frontier](design-review/150-private-project-admission-frontier.md), [Plan/2 checkpoint](design-review/152-private-plan2-classification-and-apply-checkpoint.md), [mixed checkpoint](design-review/155-private-mixed-composition-checkpoint.md), [private lock checkpoint](design-review/116-private-project-local-lock.md), [lock-first apply checkpoint](design-review/121-private-unavailable-lock-first-apply.md), [READY admission checkpoint](design-review/134-private-ready-activation-admission.md) |
| Effects and coeffects | Run/1 supplies only input, settings, attachment paths/modes, private scratch, and a finite host deadline. Host-mediated work uses `flow/call` or `effect/call`; coeffect remains theory, not another public API. The private effect switch now dispatches an exact pinned canonical Journal identity to `append`, or another exact admitted capability slot to its pinned Service Binding/export and acknowledged generation. Both paths validate protected contract values and retain durable operation closure before parent release; neither falls through to the other or forms a generic registry. Provider-specific limits belong to registered settings/grants, not a universal Run budget or wire authority report. | [Run/1 §3](spec/run-protocol.md#3-flowrun), [mixed checkpoint](design-review/155-private-mixed-composition-checkpoint.md), [Journal effect closure](design-review/146-private-journal-effect-closure.md), [Reviewed architecture §9](design-review/60-reviewed-architecture.md#9-effects-events-hooks-and-agents) |
| Events and Hooks | Events are durable immutable facts in Jig's append-only canonical Journal. The private Journal implementation proves monotonic multi-append storage, exact operation replay/conflict, authenticated source/type attenuation, atomic effect terminal and selection closure, restart recovery without redispatch, and parent aggregate closure evidence. Hooks are inert exact producer/type-to-admitted-Run-target relations with admitted half-open Journal intervals and deterministic derived-Run identity, never callbacks, middleware, or producer lifetimes. The private runtime transaction now atomically commits the Event, ordered Hook selection, unique derivations, and derived root allocation; a coalesced controller wake plus durable rescan executes READY roots promptly, and restart conservatively terminates unresolved older-epoch Hook work as `COORDINATOR_LOST` without redispatch. Hook authoring remains an explicit experimental overlay rather than part of the frozen Project Authoring SDK/1. Watchers and other producers publish through the Journal outside Hook policy. | [Journal and Hooks/1](spec/journal-and-hooks.md), [Hook authoring correction](design-review/147-private-hook-authoring-boundary.md), [Hook runtime closure](design-review/148-private-hook-runtime-frontier.md), [Journal effect closure](design-review/146-private-journal-effect-closure.md) |
| Agents | Agent Run, Session, and Interactive are separate exact Capability Contract descriptors with owner- and provider-generation-scoped authority, lifecycle, approvals, and observation. The host/operator installs a qualifying integration, the Starter/project declares logical Agent Bindings and ceilings, and Jig pins/projects/supervises them. One private checkpoint proves the finite Agent Run input/result boundary and an exact revocable logical skill view using injected deterministic test machinery. The next contained-provider step is deliberately blocked because the host has no operator-owned retained provider registration/admission substrate; a mutable fixture cannot establish one, and competing Run/1, Service/1, and private-transport sketches have not earned a public SPI. Real provider registration, containment, durable effect ownership, and a shared provider-projection ABI remain release gates. | [Agent contracts/1](spec/agents-and-semantic-choice.md), [logical projection checkpoint](design-review/157-private-agent-projection-checkpoint.md), [provider boundary](design-review/158-agent-provider-boundary.md) |
| Skills | Each exact `skills/<LocalName>/SKILL.md` subtree is opaque Flow-local source. Agent Run calls select an explicit subset (empty by default), sessions fix a subset at open, and instruction conductors select all immediate package skills. Jig projects only those subtrees read-only for the Agent owner, never copies over provider-native skills, and defines no global precedence, inheritance, or resolver. The private logical checkpoint proves empty, one-skill, and multi-skill selection, exact subtree confinement, fresh bytes, cancellation, and terminal revocation without exposing host paths. Selection proves availability, not compliance. | [Agent contracts §3.1](spec/agents-and-semantic-choice.md#31-flow-local-skills), [logical projection checkpoint](design-review/157-private-agent-projection-checkpoint.md) |
| Semantic choice | The deterministic Resolver is kernel machinery. A Flow-call slot is exact or a closed `candidates([...])`; unmapped never means open. Optional project `semanticChoice: bindingRef("<binding>")` can rank or abstain only within the admitted set after filtering. Jig Graph compiles runner-local routing to an ordinary Sley node and named links. The authoring mechanism for a changing reviewed candidate universe remains open. | [Semantic Choice §5](spec/agents-and-semantic-choice.md#5-semantic-choice), [routing sites §6](spec/agents-and-semantic-choice.md#6-two-intentional-routing-sites), [open routing note](design-review/101-default-targets-and-open-routing-candidate.md) |
| Missing dependencies | Missing, incompatible, unavailable, and ambiguous states exist without an Agent. Missing resolution is terminal for that operation; a separate admitted maintenance Run may stage repair, and only a deliberate new attempt may use the later admitted generation. | [Reviewed architecture §10](design-review/60-reviewed-architecture.md#10-resolution-and-fault-tolerance) |
| Security | Default authority is denied and mediated. Inspection preserves `requested -> wouldGrant -> planned -> realized`; Sandbox Backends receipt package containment and trusted host providers separately receipt their enforced projections. Every package begins with read-only prepared bytes and a pinned minimal Runtime Support Closure, private scratch, and protocol stdio—no ambient environment, network, extra file/descriptor, or IPC authority. Adapter-selected process views and runtime devices are exact Backend mechanisms, never package authority: views are namespace-local/read-only and devices are fresh least-mode projections. Descendants stay inside one bounded owner envelope and confer no independent authority; v1 has no process grant. Runtime Adapters plan; trusted Sandbox Backends realize inspectable predicates or fail. The private Linux proof enforces per-Run cgroup-v2 CPU/memory/PID limits, race-free pre-exec placement, nested-userns denial, Bubblewrap isolation, an outside-Run-cgroup subreaper, whole-tree kill, coordinator-independent cleanup, and zero-zombie/zero-resource residue. The proof host supplies sandbox-lifetime runtime support through bounded read-only receipts; Jig verifies but neither creates nor manages that lease. Exact Python and Bun direct recipes run real SDK-backed Run/1 components; Bun descendants use a read-only PID-local procfs and fresh private `/dev/null` and read-only `/dev/urandom`. Protected `0700` package bytes remain owned by the trusted same-UID coordinator; the root trampoline enters the Run cgroup before an exact launcher drops to that UID and starts unprivileged Bubblewrap. A durable Root Administration Run selects the exact admitted recipe without exposing host administration to FLOW, and no terminal is published before its complete Backend fence is proved. The proof publishes no Backend, Adapter, lease, or runtime-store API; cgroup v2, Bubblewrap, and agent-sandbox receipts remain implementation evidence rather than portable vocabulary. | [Reviewed architecture §11](design-review/60-reviewed-architecture.md#11-security-and-trust), [Runtime Adapters/1](spec/runtime-adapters.md), [Phase 2 proof](design-review/105-phase-2-linux-cgroup-proof.md), [direct-root closure](design-review/142-direct-root-closure-repair.md), [Bun and lifecycle closure](design-review/141-bun-and-run-lifecycle-closure.md), [runtime-support checkpoint](design-review/131-private-runtime-lease-observation.md), [Python execution checkpoint](design-review/132-private-python-run-recipe.md), [READY admission checkpoint](design-review/134-private-ready-activation-admission.md), [historical blocker](design-review/104-phase-2-security-blocker.md) |
| Lifetime and scheduling | A Run/1 process has one root wire owner, so component calls need no `ownerRequestId`; request IDs remain correlation only. Service/1 uses each pending Host mount/invocation request as the explicit cooperative owner for Provider-originated calls. Jig retains internal durable Run/Mount identities, a child-first bounded lifetime tree, wait graph, operation records, and fenced coordinator state without exposing distributed Scope/Context objects. The finite foreground session proves root-only recovery, pumping, fixed-point drain, and disposal; the mixed normal-path checkpoint additionally proves root operation closure and Service lease release before separate Mount fence/finalization under one shared coordinator epoch. End-to-end provider/coordinator-loss ordering remains open, so no restart, replacement, or daemon policy is inferred. | [Run/1 §§6–9](spec/run-protocol.md#6-operation-identity), [Service/1 §6](spec/service-protocol.md#6-owner-attributed-calls), [mixed checkpoint](design-review/155-private-mixed-composition-checkpoint.md), [finite foreground checkpoint](design-review/153-private-finite-foreground-checkpoint.md), [scheduler §12](design-review/60-reviewed-architecture.md#12-scheduler-and-lifecycle-correctness) |
| User ownership and updates | Visible source is the complete editable truth. Updates perform deterministic `BASE + LOCAL + UPSTREAM` merging before explicitly requested Agent repair; admission and pristine BASE publish and roll back as one recoverable tuple. | [Reviewed architecture §13](design-review/60-reviewed-architecture.md#13-desired-state-direct-editing-and-updates) |
| Inspection | Users can inspect source provenance, pristine BASE, local divergence, staged upstream/merge, visible and active revisions, running pins, and requested-to-realized authority without reading `.jig`. | [Authority inspection §11.4](design-review/60-reviewed-architecture.md#114-host-policy-and-inspection), [source inspection §13](design-review/60-reviewed-architecture.md#13-desired-state-direct-editing-and-updates) |
| Starters | `jig init --bare` or one copied Starter creates fully user-owned source. There is no Starter algebra or ongoing dependency; host Runtime Adapter and Sandbox preferences remain outside Starter output. | [Reviewed architecture §14.3](design-review/60-reviewed-architecture.md#143-starters) |
| Application model | Task, WorkItem, inbox, Kanban, Git/worktree, GUI, and HTTP semantics are Starter/application policy, not Jig kernel ontology. “File-native” means ordinary user-owned source, not a file-shaped application requirement. | [File ownership §13](design-review/60-reviewed-architecture.md#13-desired-state-direct-editing-and-updates), [Starters §14.3](design-review/60-reviewed-architecture.md#143-starters) |
| Runners and package names | Sley is an independent live graph runtime using function nodes, links, Flows, shared state, branch input, emit/end, fan-out/combine, retries, recovery, and inspection. Jig Graph owns semantic contracts and compiles them to ordinary Sley objects; ordinary component code composes the FLOW SDK around the graph and launches through Run/1 without a privileged Jig path. Sley is published as `@jigging/sley` for TypeScript and `sley` for Python; intended project packages are `@jigging/jig`, `@flowmd/sdk`, and `flowmd-sdk`/`flowmd_sdk`. Distribution coordinates are not protocol identities. Cordis is a Service-boundary reference only; DSH portability is not a goal. | [Sley §14.4](design-review/60-reviewed-architecture.md#144-sley), [Cordis §14.5](design-review/60-reviewed-architecture.md#145-cordis-as-a-reference-integration) |
| Distribution | Git, npm, OCI, local directories, and optional indexes are source adapters. Package identity is provenance plus exact content; no mandatory central registry owns discovery or namespaces. | [Package/1](spec/package-format.md), [Reviewed architecture §3.5](design-review/60-reviewed-architecture.md#35-canonical-package-snapshot) |
| Deferred surface | Public Scope objects, dynamic Service dependencies/exports, callback handles, streams/subscriptions, universal graph schemas, runtime profiles, persistent patch overlays, raw provider RPC, and universal sandbox claims remain removed or evidence-gated. Host-capability registration, provider projections, broader host administration and authority issuance, publication of the narrow root candidate, and open candidate-set authoring remain explicit release gates; the direct simple target is closed and the root start/status design and consumer gates pass. | [Reviewed architecture §16](design-review/60-reviewed-architecture.md#16-explicit-removals-and-deferrals), [interface gates](design-review/102-public-interface-release-gates.md) |

When a decision changes, update its normative document first and this index in
the same commit. Do not preserve rejected syntax merely because a historical
review used it.
