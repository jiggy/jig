{
  "$schema": "https://flow.dev/schemas/schema-1.json",
  "type": "object",
  "properties": {
    "delayMs": {
      "type": "integer",
      "minimum": 1,
      "maximum": 60000
    }
  },
  "required": ["delayMs"],
  "additionalProperties": false
}
