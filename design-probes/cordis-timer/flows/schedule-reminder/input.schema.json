{
  "$schema": "https://flow.dev/schemas/schema-1.json",
  "type": "object",
  "properties": {
    "timerId": { "type": "string", "minLength": 1, "maxLength": 128 },
    "delayMs": { "type": "integer", "minimum": 10, "maximum": 60000 },
    "payload": true
  },
  "required": ["timerId", "delayMs", "payload"],
  "additionalProperties": false
}

