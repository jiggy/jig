---
name: echo-upper
description: Return the supplied text converted to uppercase.
---

# Uppercase text

Read `text` from validated Run input and return its uppercase representation.
This exact Run needs no settings, slots, attachments, Agent, or ambient host
state, so its ordinary `echo-upper` Binding can be derived.
