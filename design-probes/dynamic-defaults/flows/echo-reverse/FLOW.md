---
name: echo-reverse
description: Return the supplied text with Unicode scalar values in reverse order.
---

# Reverse text

Read `text` from validated Run input and return its Unicode scalar values in
reverse order. This exact Run needs no settings, slots, attachments, Agent, or
ambient host state, so its ordinary `echo-reverse` Binding can be derived.
