---
name: focused-coding
description: Implement the smallest safe change that satisfies the current ticket.
---

# Focused coding

Trace the affected behavior before editing. Reuse the existing structure and
native tools, fix the shared root cause, and avoid speculative abstractions or
dependencies. Preserve validation, security, data safety, and explicitly
requested behavior. Leave one small runnable check for non-trivial logic.
