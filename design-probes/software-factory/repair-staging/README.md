# Repair staging

The missing-Flow maintenance Binding may write proposals here. A proposal is
not installed or admitted until ordinary review and apply succeeds. It is never
added to or used to resume the terminal failed Run; only deliberate new work
may use the later generation.
