# Factory workspace

This is the project-owned root mapped read-write only to the exact `work-agent`
Binding. The runner-local Gauntlet and Majority-Vote Flows are not Bindings,
and the Spindle process has no direct view. Runtime contents are ignored; this
file only makes the intended Starter directory visible in the design probe.
