# Broaden consumer and native-client evidence

Status: the selected standalone public repair-example attempt is recorded as an
unsuccessful, bounded qualification. Standalone dependency installation
succeeded, but review stopped before approval or Flow execution because the
example's selected Pi client was unavailable. No model call or cancellation
was attempted; cancellation was inapplicable because no Run started. This does
not establish successful independent repair adoption or native-client
qualification.

## Proposed scope

Choose one additional consumer task or native-client path from the roadmap.
Freeze public artifacts, acceptance criteria, access boundaries and any model
budget before testing. Keep independent consumer evidence separate from
maintainer co-design and deterministic transport evidence.

## Completion

The selected public task completes, including its specified unsuccessful and
cancellation cases, or its limitation is honestly recorded. This attempt's
pre-execution Pi prerequisite failure is the recorded limitation; cancellation
was not applicable. Retain the report, command transcript and artifact
identities with explicit isolation and assistance limits. A finished
unsuccessful evaluation is not an instruction to prompt-tune.

## Exclusions

This task does not defer existing source/release gates, applicable host
conformance or supported active-child cancellation guarantees. It does not
authorize a provider search, spending or another comparative benchmark.

Owners: `.agents/coordination/ROADMAP.md` determines the next outcome; `.tmp/`
retains its bounded execution plan and evidence.
